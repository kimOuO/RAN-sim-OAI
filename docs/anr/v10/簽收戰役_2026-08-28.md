# ANR v10 十二題 live 對簽戰役 —— 2026-08-28 收官

> 12/12 全簽。與 RIC 端(dt-anr-xapp 0.1.19→0.1.26、Claude 直連 anr_xapps/w)
> 以「預註冊判準 → T0 通報 → 雙方貼證據 → 對簽」協定逐題完成,約 91+ 輪通訊。

## 判定總表

| 題 | 劇本 | 輪數 | 簽收要點 | 代表性死因(修法) |
|---|---|---|---|---|
| Q1-Q3 | (前日已簽) | — | 稀疏/缺鄰/見證關係 | 聚合窗記憶、絕對稀疏限制 |
| Q4 | anr_unknown_cell | 8 | unknown∧novelCgi 錨+70.7s | barred 四路徑漏(fixture 檔)、PRB 閘、hogap 賽跑(降功率)、跨場殘留(掃+等) |
| Q5 | anr_pci_confusion | 2 | group-HO:PCI 圈人+NCI 定目標 5/5/0、TWC=0 | A3 搶跑(全場橫穿軌跡→靜態 mid 主導區) |
| Q6 | anr_xn_discovery | 1 | FLAG_SET/CLEAR/RECOVERED(cum-frozen) | 對閘先行:恢復速率按 0.1.24 判準調 |
| Q7 | anr_harmful_neighbor | 1 | 計數制偵測、L4 probe/reblock/recover | 止血抹證據(閘12 合取)→預建關係 |
| Q8 | anr_inter_freq | 3 | per-source 已知層、批 {61,68}、容器自長 | f 層熱天線;RIC 缺欄嚴格重跑(watchFirst 補齊) |
| Q9 | anr_nrt_capacity | 12 | x25 拒→x24 剪→n77 加 ×3 全窗內 | 十一種死因全存證(見 memory);出生鏈公理誕生 |
| Q10 | anr_stale_pci | 1 | CNA→再驗→REMOVE+ADD remap、恢復三面互證(證據② 修正:cum 凍結∧rate=None,原「cum 空」係 RIC 讀值名面錯置) | 換證通則誕生(succ_last50 結構不可達) |
| Q11 | anr_stale_relation | 3 | gh9 刪/old 拒+no_retry 連續證跡/tmp keep 免疫 | pre-cells NOT NULL 三連 + 三層吞錯 |
| Q12 | anr_attr_audit | 5 | 四訊號指控+清旗成對+C 段 5/5;n80 鐵律 | 增益 vs 存活拔河(雙桿解)、④=四訊號齊備 |

## sim 側烘入的機制(round_reset / anr_fixture)

- **出生鏈全序**:CellConfig → 關係 → 容量覆寫(pre 區)→ 場 → UE → 量測
- 掃+等靜默驗證(連續 90s 無新關係+量測排空)、barred 檔與場同生
- fixture:attempt_add(撞容量牆)、move_ues wait_for(等操作對象出生)、
  enforce.no_relations(④ 前限定)、cell_barred assert 與閘同源
- wire 列序 order_by(id);四路徑 barred 閘(reestab/HO/IDLE/spawn+force 409)
- 工具:field_survey.py(全場覆蓋/壓制地圖)、probe_positions.py

## 佈場公理(雙方合議,八條)

1. 引用者不得早於實體出生(含操作者/操作對象)
2. 列序是有載體的狀態 —— 判準吃哪個面就驗哪個面
3. 壓縮只適用本題分支,簽收即還原
4. 餘裕 ≫ 變異(數字是精神的字面)
5. 判準可達而缺欄 → 重跑;選錯且結構不可達 → 換證+註記
6. 【行使方算數】防線必須被行使過才算驗證 —— 未被觸發的防線與不存在的防線
   在證據上同形;驗收必須含「防線被觸發且正確動作」的正例(x25 案)
7. 【窗錨自證】時窗的錨=條件實際齊備點,由儀器自證(watchFirst 或碼下限反推),
   不是流程步驟名(Q8/Q9/Q12 三案同根)
8. 【live 為準】probe/DB/宣稱與 wire 背離時,判準只認 wire 回讀(三度驗證)
9. 【名面一致】用哪一面的名字讀哪一面的資料 —— 內部欄位名 ≠ wire 欄位名,
   拿錯名讀到的 None 不是空值是讀值錯誤(Q10 證據② 誤讀案,RIC 自首閉環)

## RIC 側合稿件(w 供稿,經直連通道)

### 版本軸(0.1.10→0.1.26,單日 17 版,每版對應觸發事件)
0.1.10 卷面 env 還原基線 → 0.1.11 逾時裁決分流 + attByTrigger 分題 →
0.1.12 Q3 深邊緣判準重寫 + Q12 訊號⑤⑥ → 0.1.13 /skips 沉默成因 + _cooled 收斂 →
0.1.14 Q1 恢復驗收(三結局)→ 0.1.15 None≠0.0 → 0.1.16 併發鎖 + acted 單調序 +
6筆≠6窗分桶 → 0.1.17 部分窗時間下限 → 0.1.18 Q2 恢復驗收 → 0.1.19 sparse 證據欄 →
0.1.20 Q12 barred 排除 → 0.1.21 watchFirstMs/persistHeldSec(三支)→
0.1.22 場紀元靜默窗 → 0.1.23 unknown PRB 全域飽和旁路 → 0.1.24 空場 watch 衛生 →
0.1.25 interfreq/xn watchFirst + attLatchedAtMs + 閘12 rate∨cum →
0.1.26 prune NOT_FOUND 續行 + newcomer-already-related 前置

### 儀器族譜(讓哪種沉默開口 + 催生事故)
observedAtMs/decisionLagMs(判定快照時刻;誤停時間軸案)· /skips 成因表
(沒偵測 vs 被閘擋;_xn_watch 查案)· ran/acted(沒跑 vs 跑了沒動作)·
watchFirstMs/persistHeldSec(從何時起數;30 秒閘三連冤案)· attLatchedAtMs
(流量背書時刻化)· epoch-hold + 空場衛生(換場即靜默;Q4 互噬戰)·
verdict 三態(失敗 vs 沒資格下結論)· 證據完整度原則(告警欄位 ⊇ 判準輸入集)

## 待辦(不擋結案)

- RIC:attr/prune watchFirst 欄位化、告警去重鍵含輪次、閘5 skip 清 watch
- sim:round_reset pre 段 fail-loud、CU 執行緒爆炸根因(25k,單發)
- 合稿:w 出版本軸(0.1.0→0.1.26)、儀器族譜、公理對照 —— 待其經直連通道送達

## 附錄:前端一鍵複測輪(2026-08-29,RIC 蓋章)

Q10 以前端 Start Sim(SimController/start)原判準複測:按鈕 16:13:33 →
prepare 自動跑(掃+等 CLEAN)→ ④ 16:17:36 → remap ④+30.8s 窗內 → 恢復 5/5。
判定:簽 + 註記(unknown/remap 賽跑兩輪皆潛伏、勝負由 CNA 節奏決定,與起場
方式無關)。**前端一鍵起場與 round_reset 等價,經 RIC 判定蓋章。**
雙方新 backlog:RIC 0.1.27(unknown 對 pci 不符者讓位 remap);sim 側
cum 欄位名與實作對齊(SinceCreation 實為 per-pair 全生涯)。

## 附錄二:前端一鍵全卷複測(2026-08-29)

可跑題 **9/9 全簽**(Q1/Q3~Q9 當日 + Q10 前日),全程前端 SimController/start 起場、
RIC(anr_xapps)即時判定。三題待 RIC 叢集接手人:Q2 重測(需 0.1.27:缺陷 A/B)、
Q11/Q12(需三項壓縮 env);RIC 待修檔 A~H 八項 + 方法論備忘(571 行)為交接依據。

當日 sim 側死因與修正:seeder 全場互種 ×2(Q8 跨頻網格→剪除面擴大;Q9 NULL gnb_id
→pre-birth 必填 gnb_id)、daphne 快取舊碼 ×2(XN_REVERSE 閘、fixture_prepare)→
「動 CU 服務碼必重啟」無例外鐵律、復活鉤 vs 清理者(續跑 hook 限伺服器行程 +
prepare 抑制閥)、Q2 pre.env 被跨場衛生抹除(clear 後補回本場宣告)。

RIC 側當日新增缺陷:A(missing 不檢查停用政策)、B(ADD 被拒無不重試帳本)、
C 收窄(唯 hogap 維度錯)、D(remap verify 無收斂)、E(訂閱斷流無自癒)、
F(ADD 被拒仍登記恢復驗收)、G(harmful 流量背書無時刻)、H(rate∨cum 未推廣)
—— 共同形狀「同模式多支各寫一份、修時只修觸發那支」,根治=抽共用函式。

新公理素材:⑩ 部署面要驗 —— 改了碼 ≠ 跑的是新碼(雙方三例);⑪ 事件時刻 vs
決策依據成立時刻(latch 量化到 guard 週期);⑫ 知道證據不完整時不可先下結論
(截斷即停);⑬ 欄位也要與場同生(gnb_id 案)。
