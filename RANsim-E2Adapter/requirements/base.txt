Django==5.0.7
djangorestframework==3.15.2
django-cors-headers==4.4.0
requests==2.32.3
python-dotenv==1.0.1
# ASN.1 codec — encodes/decodes E2AP / E2SM-KPM / E2SM-RC PDUs.
# 用 pycrate（OAI 系，E2AP information object classes 支援完整）。
# asn1tools 0.166 對 parameterized types 有 bug (TypeError 在 pre_process_parameterization)
pycrate==0.7.11
# SCTP socket — talks to RIC e2term
pysctp==0.7.2
# Async loop for SCTP read/write while Django serves HTTP status endpoints
# (pysctp is sync; we run it in a daemon thread, no extra deps needed)
